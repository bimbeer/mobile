import 'package:flutter/material.dart';

class PairsPage extends StatelessWidget {
  const PairsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pairs page'));
  }
}